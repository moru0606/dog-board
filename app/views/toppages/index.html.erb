<% if logged_in? %>
<P>ログイン完了ユーザ<%= current_user.name %></p>
 <div class="row">
   
    <div class="col-sm-8">
      <%= render 'posts/posts', posts: @posts %>
    </div>
  </div>
  
<% else %>

<div class="bg-secondary p-3 p-sm-5 my-4 rounded" >
  <div class="text-center text-white">
    <h1>French Bulldog Posts</h1>
    
    <h3>可愛いけど飼うのが大変だったりする<br>フレンチブルドッグに関する様々な情報を共有しよう</h3>
    
    <p><%= link_to '今すぐ新規登録！', signup_path, class: 'btn-btn-lg btn-primary' %></p>
  </div>
<% end %>
</div>
